# Hello World

## Java

```.sh
mkdir hello && cd hello
gradle init
  Select type of project to generate: 2
  Select implementation language: 3
  Select build script DSL: 1
  Select test framework: 1
  Project name (default: hello): [Enter]
  Source package (default: hello): [Enter]
./gradlew run
```
